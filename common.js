// 画面の座標
class Point{
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}

class Size{
    constructor(x,y){
        this.x = x;
        this.y = y;
    }
}

// 将棋盤上の座標
class Position{
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}