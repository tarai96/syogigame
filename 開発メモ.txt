将棋の駒のclassについて
phina.jsは一つの画像につき一つのスプライトクラスとして対応しているので
継承のやり方もなかったので、スプライトのメンバ変数に共通のクラスを持たせた。
DisplayElementを継承させてそれぞれの駒の子要素にしたのでthis.parentを使って駒の操作もできる
駒が成る
駒子クラスに成ったあとのスプライト（銀なら成銀）を持っておく
駒子クラスの関数で表示処理などを行った

javascriptの引数のデフォルト設定について
javascriptでは関数の引数にデフォルトの値を設定できるが
関数を呼び出すときに=で引数を指定できない
function1(arg1=0,arg2=0){
  console.log(arg1,arg2);
}
function1(arg2 = 1,arg1 = 2);
と書いても出力は
1,2
となる
しかし、javascriptでは
function1({arg1=0,arg2=0}){
  console.log(arg1,arg2);
}
function1({arg2 : 1,arg1 : 2});
とかくと出力は
2,1
になる

バグ発生：桂馬が盤面左右端にいるとき逆側にワープできてしまう

スプライトクラスからpick_piece(displayの関数)を呼び出すとthisがスプライトになっちゃう
結局、mainsceanのフラグと変数をタッチ関数で変更することにした
（親要素にアクセスした）

labelに日本語のテキストを入れたら文字化けした話
フォントのせってを変えても変化